const getters = {
  inputs: (state) => state.design.inputs,
  pickers: (state) => state.design.pickers,
  layouts: (state) => state.design.layouts,
  formDesign: (state) => state.design.formDesign,
  selected: (state) => state.design.selected
}
export default getters
